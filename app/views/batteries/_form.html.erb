<% disabled ||= false %>
<%= form_for(battery, disabled: disabled)   do |f| %>
  <%= error_explanation(battery) %>
  <%= f.collection_select :battery_model_id, BatteryModel.all, :id, :name %>
  <%= submit(f) %>
<% end %>
