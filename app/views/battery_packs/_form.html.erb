<% disabled ||= false %>
<%= form_for(battery_pack, disabled: disabled) do |f| %>
  <%= f.collection_select :battery_pack_model_id, BatteryPackModel.all, :id, :fullname %>
  <%= submit(f) %>
<% end %>
