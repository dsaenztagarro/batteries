<% disabled ||= false %>
<%= form_for(battery_pack, disabled: disabled) do |f| %>
  <% cache('all_battery_pack_types') do %>
    <%= f.collection_select :battery_pack_type, BatteryPackType.all, :id, :name %>
  <% end %>
  <%= submit(f) %>
<% end %>
