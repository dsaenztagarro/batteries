<h1>Listing Battery Types</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Device</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @battery_types.each do |battery_type| %>
  <tr>
    <td><%= battery_type.name %></td>
    <td><%= battery_type.device_name %></td>
    <td><%= link_to "Show", battery %></td>
    <td><%= link_to "Edit", edit_battery_path(battery) %></td>
    <td><%= link_to "Remove", battery, method: :delete, data: { confirm: "Are you sure?" } %></td>
  </tr>
<% end %>
</table>

<br>

<%= link_to "New battery", new_battery_path %>
