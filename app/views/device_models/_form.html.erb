<% disabled ||= false %>
<%= form_for(device_model, disabled: disabled)   do |f| %>
  <%= error_explanation(device_model) %>
	<%= f.text_field :name %>
  <%= f.collection_select :battery_size_id, BatterySize.all, :id, :name %>
	<%= f.text_field :battery_number %>
  <%= submit(f) %>
<% end %>
